---
generated_from_commit: NO_GIT_REPO (project is not version-controlled; see Unverified section)
generated_at: 2026-08-14T03:50:00+07:00
covers_paths:
  - (remote) arduino@192.168.1.41:~/.local/share/mira-so101/
  - (remote) arduino@192.168.1.41:~/mira-so101-uno-q-core/
  - (remote) arduino@192.168.1.41:~/teleop.backup-20260812-174252/
  - (remote) arduino@192.168.1.41:~/wakeword-env/
  - d:\Comp\Qualcom\mira-so101-uno-q-core.tar.gz
  - d:\Comp\Qualcom\mira-extracted\
verified_by_human: no
---

# Module: UNO Q board ("Mira")

Physical Arduino UNO Q (Qualcomm QRB2210 MPU + STM32U585 MCU), 2GB RAM, hostname `Mira`, LAN IP `192.168.1.41` (DHCP — has changed at least once this session; re-check with `hostname -I` if unreachable). Role: real-time hardware executor only. No training, no heavy AI runs here.

## Access
- SSH: key-only, alias `unoq` in Windows `~/.ssh/config` → `arduino@192.168.1.41`, key `~/.ssh/id_ed25519_unoq`.
- Sudo: passwordless (`/etc/sudoers.d/arduino-nopasswd`, `arduino ALL=(ALL) NOPASSWD: ALL`).
- Storage: two partitions — `/` (9.3G) and `/home/arduino` (3.6G, separate, chronically tight — watch free space before any pip/apt install).

## Hardware attached
- **Follower arm** (SO-101): 12.1–12.2V servos, port `/dev/ttyACM*` via `usb-1a86_USB_Single_Serial_5A7C117109-if00`.
- **Leader arm** (SO-101): 7.2–7.3V servos, port via `usb-1a86_USB_Single_Serial_5A7C118584-if00`. Intermittently fails to enumerate on the shared USB hub — suspected power/bandwidth limit on the nested hub topology (kernel logs showed `usb ...-port3: cannot reset (err=-71)` during earlier overload).
- **Cameras**: "Generic HD video" (349c:3307, `/dev/video0`) — has working built-in mic (confirmed via `arecord`), usable resolution up to 2560×1440 MJPG, streamed live via `ustreamer --resolution=640x480 --desired-fps=10` on port 8080 (1280x720 crash-looped, 640x480 is stable). "Sunplus SPCA2085" (1bcf:0b09, `/dev/video2`) — wrist-mounted — **confirmed dead at the hardware/protocol level, not a config/power issue**: exhaustively tested via `fswebcam`, `ustreamer` (all resolutions incl. its native 160x120), raw `v4l2-ctl --stream-mmap` (hangs indefinitely with zero output), and re-tested identically on Windows via DirectShow (same driver family, `usbvideo.sys`, not a vendor driver as originally hoped) — every path produces either a stable black frame or a fixed one-line color-noise-then-black pattern, never real image data, deterministically across cable/port replug. Do not spend further debug time on this unit; replace it (OV5640-based UVC module available locally in VN via Hshop.vn/mlab.com.vn/pivietnam.com.vn; OV9782 global-shutter is the internationally-ordered ideal but not locally stocked).
- 2× Huasheng USB2.0 hubs, nested (hub B plugged into hub A). This nesting is the likely root cause of the recurring "second device won't enumerate" pattern. **Superseded 2026-08-14** by a Genesys Logic-chipset hub (proper USB2 4-port hub + USB3 hub w/ built-in AX88179 gigabit ethernet) — single hub, not nested, with its own power adapter now on a wall outlet (was previously mistakenly fed from a power bank, which likely under-delivers current under load spikes).
- **USB mic**: BOYAMIC (Actions Semiconductor, `10d6:b011`) — works, but only in a specific configuration found by trial: **S24_3LE format, 2 channels, 48000Hz, real signal on the RIGHT channel only (LEFT is silent)**; rejects 16-bit and mono requests outright (`arecord` errors "Sample format non available" / "Channels count non available"). ALSA capture gain is already fixed at 100% — there is no software gain knob to raise for this device.
- **Board's own USB-C port requires a genuine data-capable cable, not charge-only**: after a hub+mic rewiring session, `lsusb` briefly showed *zero* devices at all (not even the hub) despite the board itself staying up or (with a bad cable) even losing power - Arduino's own docs only generically warn to use "a USB-C dongle/cable with data capability," not charge-only. If a hub disappears entirely from `lsusb` after reseating a cable, suspect the cable before the hub.
- **Board-wide power crash** (root-caused): plugging in a 2nd camera while the follower arm + 1st camera + hub were already connected caused the *entire board* to lose power and reboot uncleanly (confirmed via `dmesg`/`systemd-journald` corruption messages showing a real early-boot log, not just a USB subsystem reset). Root cause: the UNO Q's single USB-C port both powers the board *and* back-feeds the downstream hub/device chain — total draw of 2 cameras + arm + hub exceeds what one port sustains; a stronger upstream supply alone did not fix it (the nested hub itself isn't independently powered).
  - Official spec (`docs.arduino.cc/tutorials/uno-q/power-specification/`): board draws a fixed **5V/3A (15W)** via USB-C PD — it only ever negotiates this one profile, never higher-voltage PD tiers, so a higher-wattage charger (e.g. a 65W GaN unit) provides *no* extra headroom on the 5V rail unless that specific charger's 5V rail itself exceeds 3A (check the printed spec table; most don't — the 65W is only reachable at 20V). Docs explicitly warn: "if the supply current-limits during peaks, voltage hang may cause resets" — matches the observed crash signature exactly.
  - Whether powering via the board's `+5V`/`VIN` header pins (JANALOG connector: `...→ +3V3 → +5V → GND → GND → VIN →...`) would free the USB-C port for host-only duty was investigated and **does not work as hoped**: per Arduino forum reports, powering via the header pins puts the USB-C controller into device mode by default, so it stops supplying VBUS to downstream hubs at all. Do not pursue this path — give the hub independent power instead (done, see above), keep the board itself on USB-C.

## Software installed
- System Python 3.13 (Debian on the board), `~/lerobot-env` was created and later **deleted** (freed ~1.1GB; superseded by Mira core below).
- **Mira SO-101 Motion Core** (`~/.local/share/mira-so101/`, own venv): minimal runtime, no torch/opencv by default. CLI: `mira-robot {doctor, teleop, replay <name>, list, status, stop}`. Motions bundled: wave, dance, nod/yes, shake/no, play-dead, clean, scan — all verified working end-to-end on the follower arm (voice-triggered via SSH from Windows too, see `windows-pc.md`).
  - `opencv-python-headless` was added to this venv (2026-08-14) specifically to unblock `lerobot-calibrate`, which imports camera config classes unconditionally even though calibration itself never touches a camera. Full `lerobot-calibrate` still won't run here (needs torch/datasets/gymnasium/etc. that are deliberately absent) — use the two small standalone helper scripts below instead, which only touch the already-working `SO101Follower`/`bus` classes.
  - `calibrate_follower.py` and `read_follower_state.py` (both copied to `/home/arduino/`, reuse `runtime.py`'s `make_arm`/`detect_single_role`/`calibration_path`): the former drives `SO101Follower.calibrate()` interactively over plain SSH (no LeLab, no physical replug needed — this is how you recalibrate the board-connected arm without moving it to Windows); the latter is read-only (`get_observation()`, disconnects with `disable_torque=True`, never writes to motors) and is what the Windows shadow-inference client uses to get real joint state.
- **openWakeWord + Vosk** (`~/wakeword-env/`): used for on-device benchmarking (see PIPELINE.md for numbers). Not yet wired into `mira-robot`. Vosk was later abandoned project-wide in favor of Whisper/PhoWhisper (accent-recognition failures) — see `windows-pc.md`.
- **`camera_exporter.py`** (`/home/arduino/camera_exporter.py`, plain `python3`, stdlib only — no deps installed, by design, given the board's tight RAM): Prometheus-format `/metrics` on `:9101`, polls the overhead camera's `ustreamer` snapshot every 10s (`camera_up{camera="overhead"}`) and reports the wrist camera as a static known-broken gauge. Started via bare `nohup`, not a systemd service — will not survive a reboot.
- **Board-native voice pipeline** (`/home/arduino/board_wakeword.py`, added to `~/.local/share/mira-so101/venv/` on 2026-08-14: `openwakeword`, `sounddevice`, `onnxruntime`, `scipy`, `sherpa-onnx`, `prometheus_client` — plus system `libportaudio2` via apt, required or `sounddevice` raises `OSError: PortAudio library not found`). This runs the *entire* wake-word → STT → motion loop locally on the board, no SSH/network hop, no Windows PC involved for the canned-command path — the architecture originally sketched as a "target" is now real and verified:
  - Wake word: same `mira.onnx` model as the Windows build (copied to `~/.local/share/mira-so101/mira.onnx`), same `openwakeword` package (0.4.0, bundles its own base models, unlike the Windows/0.6.0 install which needed `download_models()`).
  - STT: **`hynt/Zipformer-30M-RNNT-6000h`** via `sherpa-onnx` (`OfflineRecognizer.from_transducer`, int8 ONNX, `~/zipformer-vi/` — encoder/decoder/joiner .onnx + a `tokens.txt` generated from the repo's `bpe.model` via `sentencepiece`, since the repo doesn't ship tokens.txt directly). Chosen to replace PhoWhisper project-wide: 30M params vs 769M, and measured end-to-end STT latency **~700-735ms on the board's own aarch64 CPU** for a ~4s clip (vs PhoWhisper's ~1-2s on an RTX 3070) — smaller and faster despite running on much weaker hardware. Verified live, multiple times, with exact-match transcriptions ("mira gật đầu", "mira nhảy") triggering the correct motion with zero alias-table massaging needed.
  - Because BOYAMIC only exposes S24_3LE/2ch/48kHz (see above), audio capture does **not** use `sounddevice`'s stream API directly — it shells out to a persistent `arecord -D hw:CARD=BOYAMIC,DEV=0 -f S24_3LE -c 2 -r 48000 -t raw` subprocess and reads fixed-size raw chunks from its stdout pipe, manually decoding 24-bit LE, keeping only the right channel, and downsampling 48kHz→16kHz with `scipy.signal.resample_poly` before feeding openwakeword/sherpa-onnx (both want 16kHz mono int16). This mirrors the equivalent WASAPI fix needed on Windows (see `windows-pc.md`) — same underlying problem (device won't accept the model's native 16kHz/mono format), different platform-specific plumbing.
  - Motion execution is a plain local `subprocess.run(["mira-robot", "replay", motion, "--yes"])` — no `ssh` invocation at all, since the script already runs on the board.
  - Metrics on `:9103` (kept distinct from the Windows build's `:9101`/`:9102` so both could be scraped by the same Prometheus instance if wired up later — not yet added to `prometheus.yml`).
  - Not yet a systemd service / not yet auto-started — currently run manually via SSH for testing, same `nohup`-survivability caveat as `camera_exporter.py` applies if this is left running unattended.
  - **Wake-word retrain (2026-08-14) — root cause found, openWakeWord abandoned for this project.** The custom "mira" classifier was trained only on synthetic Piper TTS voices and missed real "Mira" calls often enough to be the pipeline's actual bottleneck. First attempt: collect 40 real positive samples from this user via `voice-control/collect_mira_samples.py`, mix into the RTX 4090 training set, bump `layer_size` 32→128, retrain (`gpu-training-server.md` has the training-side details; result was 96.08% accuracy / 92.22% recall / 0.27 FP/hr *by openWakeWord's own training-time metrics*). **That metric was misleading.** Live testing on the board told a different story, worked through in this order:
    1. Deployed and live-tested: real "Mira" calls triggered inconsistently (worked once at 0.837, then several runs found nothing at all).
    2. Ruled out the mic/hardware: a raw `arecord` capture during a "silent" run showed strong, correctly-decoded signal (right-channel RMS ~42000/24-bit-scale) — the audio pipeline itself was fine.
    3. Ruled out "too quiet" and "handling noise" as the explanation: added temporary per-chunk RMS/score debug logging directly in `board_wakeword.py`; clean, loud, speech-shaped audio (RMS 2000-9000, comparable to the one successful trigger) still produced a flat `score≈0.0003-0.0009`, never once crossing even the 0.3 debug-print floor.
    4. Ruled out a broken export: probed openWakeWord's internal melspectrogram/embedding buffers directly (`mel std=3.16`, `emb std=16.1` — healthy, varying features) and ran the classifier head manually on those features — still ~0.0004. The shared feature extractor was healthy; the classifier itself simply wasn't responding.
    5. Confirmed with a real recognition test: fed the *same* live-captured audio through Zipformer, which transcribed it perfectly ("MIRA VẪY TAY MIRA VẪY TAY..."). So the audio was unambiguously good speech, correctly captured and decoded — and both the new (layer_size 128) and the old (layer_size 32) classifier scored ~0/40 recall on the actual 40 real training samples themselves, back to back, on the same hardware.
    6. Root cause, finally: the classifier was trained on **isolated single-word "Mira" TTS clips**, so it only ever learned to fire on a fully isolated, cleanly-bounded utterance of the word alone — not "Mira" embedded in a natural phrase like "Mira vẫy tay" (confirmed: the one clean trigger, 0.837, was an isolated "Mira" with no command word attached). Separately, `collect_mira_samples.py`'s 1.6s recording window was measured to be too short — real onsets landed 0.42-1.26s in and energy ran to the very last frame on many takes, i.e. the *tail* of "Mira" was being cut off in the training data itself, compounding the problem. Both are fixed in the script (window widened to 3.0s, an explicit tail-energy check now rejects and retries any take that's still making sound at the end) but a fresh retrain wasn't attempted — see the pivot below instead.
  - **Pivot: `board_voice_control.py` replaces `board_wakeword.py`+openWakeWord entirely.** Rather than chase a wake-word classifier that needs the word said in isolation (an unnatural way to talk to something), the already-excellent, already-verified Zipformer ASR now does both jobs: a rolling 2.5s window (0.5s hop) is transcribed continuously, and a motion fires when the transcript contains both the wake word and a command word together (e.g. "mira vẫy tay"). Measured on-board decode latency by window size: 1.0s→182ms, 1.5s→263ms, 2.0s→339ms, **2.5s→426ms**, 3.0s→500ms (4 cores, `num_threads=2`) — comfortably fast enough for a 0.5s hop.
    - **Fuzzy wake matching**: "Mira" is a foreign name to a Vietnamese ASR model and comes back mangled in real transcripts — observed in one 10s clip alone: mira, vira, bida, mi rươ, willer, willia, quy lơ, quy lê. An exact-string list can't keep up, so `heard_wake_word()` falls back to a `difflib.SequenceMatcher` ratio (≥0.6) against "mira" over single tokens and adjacent token pairs.
    - **Two-word command phrases bypass the wake-word gate entirely**: on the same 10s test clip, the ASR read the command phrase "vẫy tay" correctly in 8/13 overlapping windows but the wake word correctly in only about half of those — so requiring both together was actually *dropping* good triggers. A ≥2-word command phrase ("vẫy tay", "lắc đầu", ...) is specific enough to act on by itself; short single-word commands (`có`, `không`, `gật`, `lắc`) still require the wake word, since those are common enough in ordinary speech that acting on them alone would misfire on unrelated conversation.
    - Validated (not yet with the user live, but rigorously) by replaying a real captured audio clip containing 4 natural "mira + command" utterances through the actual matching logic offline: 8/8 of the windows that should act, did.
    - Debug WAVs of anything that fires now save to `~/voice_debug_logs/trigger_*.wav`. Metrics stay on `:9103` (gauges/counters renamed: `board_audio_rms`, `board_decode_ms`, `board_voice_triggers_total`, `board_voice_recognized_total{motion}`).
    - `board_wakeword.py` and the retrained `mira.onnx` are kept on disk (`mira.onnx`, `mira.onnx.new_layer128`, `mira.onnx.bak_layer32_20260814` all present in `~/.local/share/mira-so101/`) as a fallback / for future reference, but are not the active pipeline.
- **Bluetooth audio output: BlueALSA, not PipeWire (2026-08-14, working end-to-end)**. The board has a working onboard BT radio (`hci0`, adapter named "Mira") and pairs/trusts a real speaker (JBL Go 4, `4C:3C:8F:3C:42:EE`) fine, but **PipeWire/WirePlumber never worked as the A2DP sink** despite a long diagnosis: installed `pipewire pipewire-pulse libspa-0.2-bluetooth wireplumber`, enabled linger, confirmed `wpctl status` saw the adapter, confirmed the `bluez.lua` monitor script loaded with no error, confirmed D-Bus policy/polkit were both present and unremarkable, even enabled BlueZ's `Experimental = true` (the classic fix for this class of bug) and did a fresh remove+re-pair — every single connect attempt failed identically with `org.bluez.Error.Failed br-connection-profile-unavailable`. `bluetoothd`'s own journal pinned the exact cause: `a2dp-sink profile connect failed... Protocol not available` — nothing was ever registering as an A2DP sink profile with bluetoothd, for reasons not fully root-caused (plausible: a packaging gap in this board's Debian derivative; WirePlumber 0.5.8 + BlueZ 5.82 + PipeWire 1.4.2 on a headless SSH-only session is not a heavily-tested combination).
  - **Fix: `bluez-alsa-utils`** (the `bluealsa` package) — a lightweight daemon built specifically for headless BT audio, bridging BlueZ directly to ALSA with no PipeWire/session/seat model involved. `sudo apt-get install bluez-alsa-utils` installed and auto-started `bluealsa.service` (system-level) — the very next `connect` attempt succeeded immediately (`Connection successful`, real SEP/transport objects appeared). **PipeWire/WirePlumber are now stopped and masked** (`systemctl --user mask wireplumber pipewire pipewire-pulse pipewire.socket pipewire-pulse.socket`) so they can't compete with bluealsa for the audio hardware — they weren't doing anything useful anyway once bluealsa took over.
  - **Playback command, confirmed working**: `aplay -D "plug:'bluealsa:DEV=<MAC>,PROFILE=a2dp'" <file>.wav` — the `plug:` wrapper auto-converts sample rate/channels (tested successfully going straight from Piper's native 22050Hz mono to the link's 48kHz stereo, no manual resampling needed). **Do not use `bluealsa-aplay`** for this — that tool plays audio *from* a Bluetooth source *to* local ALSA (the reverse direction, e.g. for a BT microphone); it will start a "main loop" with no error and produce nothing audible, which cost real time to figure out.
  - **Two Piper TTS quality bugs found and fixed by ear, both in `mira_chat_server.py`**: (1) "Mira" spoken plainly gets phonemized with English phones (`ˈi ə ɹ ə`) this Vietnamese-only acoustic model never saw in training, so it improvises something close to the real word "mỉa" instead - confirmed by direct comparison via `piper.phonemize_espeak.EspeakPhonemizer` before fixing it, not just guessing. Splitting it into two space-separated words, "Mi ra", forces plain Vietnamese-syllable phonemes the model knows (confirmed by ear once fixed). A hyphen ("Mi-ra") gives the *same phonemes* but reads unnaturally rushed, like one word - use a space. (2) Every reply had an audible click/pop at the very end (classic ALSA buffer-underrun-on-drain) - fixed by appending 0.3s of trailing silence to the synthesized WAV before it's returned.
  - **A real concurrency bug found via live testing**: two "Mira" calls close together (no command word either time) both fired `chat_and_speak()` as a fire-and-forget background thread with no gating, and both landed on the GPU-bound LLM around the same time - one or both then exceeded the 8s client timeout and produced no reply at all. Fixed with a `chat_busy` threading.Event (same pattern as the existing `robot_busy` gate for motions): a new wake-only trigger is ignored with a printed message while a previous chat round-trip is still in flight, rather than firing a second overlapping request.
  - `board_voice_control.py`'s `CHAT_SERVER_URL` points at the Windows PC (`http://192.168.1.32:8766/chat`), **not** the 4090 directly — the board's own network blocks outbound SSH to anywhere but port 443 (confirmed: port 234 and even plain port 22 both time out from the board, port 443 works), so a board→4090 tunnel is a dead end regardless of SSH key trust. The Windows PC already has a working SSH path to the 4090 and opens `ssh -N -L 0.0.0.0:8766:localhost:8766 61.28.228.23` (bound to all interfaces, not just localhost, specifically so the board can reach it over the LAN) - proxying through it isn't a workaround, it's the only viable long-term path given that network restriction (confirmed with the user). This tunnel is currently a bare background process on Windows, same non-survivability caveat as everything else bare-`nohup`'d in this project.
  - **Verified live end-to-end** (2026-08-14): real "Mira" calls correctly triggered both canned motions (`mira-robot replay yes`, `mira-robot replay shake`, both completed) and the chat fallback, with corrected pronunciation and clean playback, over the actual JBL speaker.
  - **Known gap, not yet addressed**: no acoustic echo cancellation. If Mira's own TTS reply is still playing when the mic starts listening again, it could pick up its own voice as new input. Low risk today since motion-triggering still requires the wake word + a real command match in the same transcript window, but worth a real fix (e.g. muting capture during playback) before this runs unattended for long stretches.
  - **Gesture proposals for the conversational layer**: see `docs/gesture_proposals.md` — `thinking` and `shrug` matter most functionally (filling the LLM's generation latency, and the natural fallback body language when the LLM itself is uncertain); the rest are polish. None of these are recorded yet — next session, teleop each through LeLab and save under `hf_lerobot/local/motion_<name>/` matching the existing bundled motions' structure, same as any other canned motion.

## Known issues (live)
1. Calibration files (`~/.cache/huggingface/lerobot/calibration/.../my_follower.json`, `my_leader.json`) drift from the physical servos' actual on-chip registers over time. Fixed once this session by reading `bus.read_calibration()` directly off the servo. The follower's calibration was **recalibrated again on 2026-08-14** via LeLab (Windows) + `calibrate_follower.py`, then synced to `my_follower.json` (old one backed up alongside it as `my_follower.json.bak_<timestamp>`) — this was in response to diagnosing that the bundled canned motions (wave/dance/etc., stored as LeRobot-normalized ±100 values) drift in amplitude whenever the calibration they're replayed under differs from the calibration active when they were originally recorded. **Recalibrating alone does not fix this** — replaying `motion_dance` under the new calibration triggered heavy `max_relative_target` safety-clamping (e.g. `shoulder_lift` target -99 clamped down to roughly -32 to -48 across several frames), meaning the canned motions now need to be **re-recorded** under the current calibration to restore correct amplitude. Leader's JSON was separately replaced with a fresh LeLab export earlier — not yet re-verified with the leader physically connected.
2. Second-arm / second-camera enumeration failures recur under load — always check `lsusb` + `dmesg | tail` before trusting a "not connected" error from `mira-robot`. See the board-wide power crash root cause above.
3. VSCode Remote-SSH server processes (`.vscode-server/cli/...`, including a headless Copilot language server) have been observed consuming up to ~1.1GB RSS on this 2GB board. Close the Remote-SSH window when not actively using it.

## Human corrections
<!-- survives regeneration. agents must carry this block forward verbatim -->
(none yet)
